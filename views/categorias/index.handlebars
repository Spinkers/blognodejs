<div class="card">
    <div class="card-body">
        <h2>Categorias: </h2>
        <hr />
        {{#each categorias}}
        <a href="/categorias/{{slug}}"><h5>{{nome}}</h5></a>
        {{else}}

        {{/each}}
    </div>
</div>